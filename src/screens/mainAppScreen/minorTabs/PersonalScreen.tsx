import {IPersonalScreen} from '@utils/interfaces';
import React from 'react';
import {Text, View} from 'react-native';

const PersonalScreen = (props: IPersonalScreen) => {
  return (
    <View>
      <Text>settings</Text>
    </View>
  );
};

export default PersonalScreen;
