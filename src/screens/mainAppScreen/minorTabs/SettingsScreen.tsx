import {ISettingsScreen} from '@utils/interfaces';
import React from 'react';
import {Text, View} from 'react-native';

const SettingsScreen = (props: ISettingsScreen) => {
  return (
    <View>
      <Text>settings</Text>
    </View>
  );
};

export default SettingsScreen;
